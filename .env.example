# Google AI Studio API key for Nano Banana (gemini-2.5-flash-image) — used by scripts/generate-images.ts ONLY.
# The runtime app does not call any AI API; images are pre-generated and baked into public/training/.
GOOGLE_GENAI_API_KEY=
